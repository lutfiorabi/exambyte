logging:
  level:
    org:
      springframework:
        security: info

spring:
  datasource:
    password: iamgroot
    username: rop
    url: jdbc:postgresql://localhost:5432/exambyte
  application:
    name: ExamByte
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${CLIENT_ID}
            client-secret: ${CLIENT_SECRET}
            scope: read:public_repo